// Single source of truth for the firmware's protocol/semver reply.
//
// Keep in sync with CHANGELOG.md and host/pyproject.toml (see CLAUDE.md,
// "Documentation" and "Semver rules").
#pragma once

#define USBGPIO_VERSION "0.1.0"
